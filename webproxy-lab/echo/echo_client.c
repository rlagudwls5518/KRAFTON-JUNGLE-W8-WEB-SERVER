#include <stdio.h>
#include "csapp.h"

int open_clientfd(char *hostname, char*port);

int main(int argc, char **argv) {
    // 여기에 코드를 작성하세요
    exit(0);
}

int open_clientfd(char *hostname, char*port) {
    // 여기에 코드를 작성하세요
    return -1;
}